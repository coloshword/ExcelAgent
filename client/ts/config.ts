// config.ts: some configurations for the client. Make sure it matches the config.json file 
const config = {
    "server_uri": "http://127.0.0.1:8000",
    "client_uri": "http://127.0.0.1:5500/client",
}

export default config;